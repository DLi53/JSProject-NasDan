const axios = require('axios');
const { Chart } = require('chart.js');

// const { Client } = require("iexjs");
// const client = new Client({ api_token: "sk_3d2eb0ffc27c4c3bbfb0a159f4a0112d", version: "v1" });

export const stocks = {}
class ChartData{
    constructor(symbol, range = "1m") {
        this.symbol = symbol;
        this.range = range;
        // this.dates.
        // this.fatch = fatch.change
    }


} 
